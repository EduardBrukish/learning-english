const colors = {
  blue: '#300199',
}

const elementColors = {
  backgroundColor: colors.blue
}

const theme = {}

theme.colors = {
  ...colors
}

theme.elementColors = {
  ...elementColors
}

export default theme